{
  "name": "duimp-converter",
  "version": "1.0.0",
  "description": "Conversor DUIMP (PDF) + Espelho NF (XLSX) para XML",
  "private": true,
  "scripts": {
    "dev:backend":  "cd backend && node server.js",
    "dev:frontend": "cd frontend && npm run dev",
    "build":        "cd frontend && npm install && npm run build",
    "install:all":  "cd backend && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "cors":        "^2.8.5",
    "express":     "^4.18.2",
    "multer":      "^1.4.5-lts.1",
    "pdf-parse":   "^1.1.1",
    "xlsx":        "^0.18.5",
    "xmlbuilder2": "^3.1.1"
  }
}
